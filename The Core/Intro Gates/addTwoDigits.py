def addTwoDigits(n):
    string = str(n)
    first= int(string[0])
    second= int(string[1])
    return first+second
    
'''You are given a two-digit integer n. Return the sum of its digits.

Example
For n = 29, the output should be
addTwoDigits(n) = 11.'''
